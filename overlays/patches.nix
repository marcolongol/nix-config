final: prev: {
  # Package patches go here
  # Example:
  # somePackage = prev.somePackage.overrideAttrs (old: {
  #   patches = (old.patches or []) ++ [
  #     ./patches/fix-something.patch
  #   ];
  # });
}
